snapshots:
    - name: dim_hosts
      relation: ref('hosts')

      config:
        target_schema: gold
        unique_key: HOST_ID
        strategy: timestamp
        updated_at: HOSTS_CREATED_AT
        dbt_valid_to_current: "to_date('9999-12-31')"

